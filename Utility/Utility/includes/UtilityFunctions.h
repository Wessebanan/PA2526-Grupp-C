#pragma once
#include <DirectXMath.h>

namespace UtilityFunctions {
	void dankPlaceHolder();
	/* Generates a world XMMATRIX from the given TransformComponent */
	//DirectX::XMMATRIX GetWorldMatrix(); //(ecs::components::TransformComponent tc);
}